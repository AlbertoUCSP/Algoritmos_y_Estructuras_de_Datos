#include <iostream>

using namespace std;

int main() {
    /*CVector digitos;
    int m;
    cout << "Ingresa un numero: ";
    cin >> m;
    
    int suma = 0;
    int resultado = m * 27;

    while (resultado / 10 != 0) {
        int digito = resultado % 10;
        digitos.push_front2(digito);
        resultado = resultado / 10;
        suma += digito;
    }

    digitos.print();

    int x = 0;
    while (suma % 9 != 0) {
        suma += 1;
        x++;
    }

    digitos.push_front2(x);
    digitos.print();*/

    return 0;
}